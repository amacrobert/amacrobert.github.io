// Applies to every article in this directory (and its subdirectories), so a new
// article only needs its own front matter — no layout or tag boilerplate.
module.exports = {
    layout: "article.njk",
    tags: ["articles"],
};
